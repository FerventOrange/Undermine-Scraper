# docker-compose.yml
services:
  undermine-scraper:
    build: .
    container_name: undermine-scraper
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
